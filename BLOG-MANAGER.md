# Seven 博客工作台

## 启动

双击仓库根目录的 `manage-blog.cmd`。需要 Python 3.10 或更高版本，不需要 Node.js、数据库或额外 Python 包。

浏览器会自动打开 `http://127.0.0.1:8765/manager/`。终端窗口需要保持运行，按 `Ctrl+C` 停止。

也可以在仓库根目录运行：

```powershell
python scripts/blog_manager.py
```

如果端口被占用，可使用 `python scripts/blog_manager.py --port 8766`。不希望自动打开浏览器时添加 `--no-browser`。

### 菜单有了，却看不到编辑按钮？

这通常是旧 Python 服务还在运行：HTML/JavaScript 会从磁盘加载新版，但已运行的后端不会自动重载。新版会在页面顶部明确提示版本不匹配，而不是一直显示「资料尚未加载」。请关闭所有旧的工作台运行窗口，再重新双击启动文件；仅刷新浏览器或反复双击，不会更新旧服务。

新版服务独占端口，避免 Windows 下重复启动导致多个旧服务共用 8765。重复启动同一版本会打开已有工作台；检测到旧版本或其他服务占用时会给出明确提示，不会静默连接旧版。也可用 `--port 8766` 启动，并访问终端显示的新地址。

新版「游戏作品」右上角是「新增游戏作品」，每个作品卡片下方可编辑；「关于页面」右上角直接提供「编辑关于页面」。普通博客的 `/projects/`、`/about/` 是展示页面，编辑请在 `/manager/` 工作台中进行。

## 能管理什么

- **概览**：文章、草稿、图片及字数统计，最近文章与快捷操作。
- **文章**：搜索标题/正文、按状态筛选、新建与编辑游戏评测，设置日期、评分及封面。
- **正文编辑**：插入粗体、删除线、图片；正文预览复用真实的博客生成器。支持 `Ctrl+S` / `Cmd+S`，关闭未保存的文章时会提醒。
- **图片**：搜索本地素材，批量上传 PNG/JPEG/GIF/WebP（每张最多 12 MB），按文件内容去重，选择封面、插入正文、复制图片引用，以及保留原路径替换素材。为避免误删正在使用的图片，暂不提供图片删除。
- **草稿与回收站**：草稿不生成网页；已收录文章可转为草稿或移入回收站，同时清理本地页面及索引。恢复后先进入草稿箱。
- **生成与预览**：保存已收录文章会生成文章页并更新首页、游戏栏目、分类、标签、归档、搜索索引与数量。可单独重新生成或预览本地站点。
- **内容备份**：下载含文章源文件、所有图片、草稿、回收站及历史版本的 ZIP。
- **游戏作品**：新增、修改、移除、调整展示顺序；编辑介绍、封面、标签、开发状态、引擎、职责，以及 Steam/游玩/下载/源码链接。
- **关于页面**：编辑展示名称、个人简介、头像、正文和个人链接，保留现有主题外壳并更新站内搜索。
- **云端同步**：检查文件和远端状态，列出变更与待推送提交，确认后提交并推送到当前博客的 GitHub main 分支。
- **样式与颜色**：取色器或 HEX 输入修改切换动画颜色、主题强调色、悬停色、浅色和深色模式的背景与卡片颜色；调整卡片圆角、正文字号、内页头图高度和头图过渡时长。支持浅色 / 深色实时示意预览和恢复默认。

## 样式与颜色

1. 升级后先关闭旧工作台进程，重新双击 `manage-blog.cmd`。
2. 打开「样式与颜色」→「编辑样式与颜色」，默认切换动画颜色为 `#39c5bb`。
3. 颜色需为六位 HEX，例如 `#39c5bb`。取色器与文本输入同步；数值输入有范围限制，过渡时长 `0` 表示关闭头图过渡。
4. 「恢复默认」仅更新编辑器，关闭未保存的窗口会提示确认；点击「保存并生成」后才修改本地站点。
5. 打开真实博客预览，确认后到「云端同步」检查并发布。示意卡片不是完整博客截图；真实切换遮罩会渐隐，保留原有透明头图与背景图片，首页保持一屏，内页头图高度不影响首页和文章封面。

配置保存在 `content/appearance.json`；生成器输出 `css/appearance.css`，为所有公开 HTML 插入同步加载、带内容版本号的样式链接，避免旧底色闪现和缓存未更新。无需手动编辑生成的 CSS。保存沿用历史备份、并发修改检测与失败回滚；不会自动提交或推送。

## 一次完整的写作流程

1. 点击「写一篇文章」，填写标题和唯一英文路径，例如 `my-new-review`。
2. 写正文并设置 0–10 分的评分。评分会写入或更新正文中的 `游戏评分：8`。
3. 从素材库选择封面，正文插图使用 `/img/games/` 下的图片。上传的图片自动放在该目录下。
4. 未完成时「保存草稿」；完成后「保存并生成」。路径首次保存后锁定，防止意外改变文章地址。
5. 点击文章的「预览」，检查本地博客效果。
6. 进入「云端同步」，点击「检查变更与远端」，审核文件及已有提交，再点击「确认提交并推送 GitHub」。也可继续使用自己的 Git 工具。

**「已收录」表示加入本地静态站点，不代表已上线。普通保存不会提交或推送；只有在「云端同步」确认发布后才运行 Git。工具不会修改 Notion。**

## 云端同步

工作台默认绑定本仓库的 `origin`：`https://github.com/Seven39c5bb/Seven39c5bb.github.io.git`，分支为 `main`。使用本机已有的 Git 凭据，不要求在网页填写或保存 GitHub 密码、令牌。

- 首次使用前，在自己的 Git 工具或终端登录 GitHub，并配置 `user.name`、`user.email`。登录未完成时工作台会报错，不会弹出交互式凭据输入。
- 「检查变更与远端」先生成本地页面，再 fetch 检查远端；不会自动 pull、切换分支或解决冲突。
- 如果远端有本地未包含的提交，或 Git 正在合并/变基，先在 Git 工具中处理，工作台拒绝强制覆盖。
- 暂存区必须为空，避免误提交其他正在处理的修改。请先自行提交或取消暂存（保留工作区文件），然后重新检查。
- 确认列表包括公开站点内容、素材及工作台代码。`.blog-manager/`、隐藏目录、Python 缓存、未知类型的文件不加入本次提交；已有待推送提交会完整推送，不会过滤其历史内容。请不要自行提交私密草稿或凭据。
- 发布列表有效期 10 分钟；确认后如文件、分支或远端发生变化，将要求重新检查。
- 推送失败时已创建的本地提交仍保留。解决登录或网络问题后重新检查、推送，不需要重复编辑。
- 推送成功只表示代码进入 GitHub。静态网站是否完成更新，需要在 GitHub 仓库的 Actions / Pages 中确认；本工具不更改 Pages 配置。

这是**从本机到 GitHub 的单向发布**，不是多电脑自动双向同步。发布前仔细检查文件列表：上传在 `img/` 下的图片也会公开，即使仅用于草稿。已有本地提交会一并推送，请一并确认。

## 游戏作品与关于页面

「游戏作品」沿用 `content/projects.json` 的现有数据和作品页生成器，作品增删及顺序变更保存后更新 `/projects/` 和搜索索引。移除作品不会删除其图片或可下载文件；修改前的 JSON 会保存在历史目录中。

「关于页面」首次保存时创建 `content/about.json`，生成 `/about/`。正文支持分段、`**粗体**` 和 `## 二级标题`，个人链接使用 `名称 | https://地址` 每行一个。头像从现有素材中选择，也可以先去素材库上传。本功能只修改关于页，不改变全站侧栏头像。

## 文件与安全

### 替换图片素材

1. 在左侧进入「图片素材」，搜索要替换的文件，点击卡片上的「替换图片」。
2. 选择电脑上的新图片，在弹窗中对照查看原图和新图，核对尺寸与文件大小。
3. 点击「确认替换」。工具先备份旧文件，再用新内容覆盖原路径，不需要逐篇修改文章引用，也不需要重新生成页面。

支持 PNG、JPG/JPEG、GIF、WebP，以及已有 ICO 图标的替换；每张最多 12 MB。为了保持扩展名与实际格式一致，只能同格式替换（JPG 与 JPEG 可互换）。新文件叫什么不影响原图文件名。需要换成另一种格式时，请先上传新素材，再在文章编辑器中选择它。

替换会影响所有使用该地址的封面、正文、头像或主题图片。若只想修改某一篇文章，请上传为新素材，不要替换共用图片。相同内容不重复覆盖；素材已被其他窗口修改时会拒绝旧版本请求。

旧图片保存在 `.blog-manager/history/images/时间戳/img/原图片相对路径`，并包含在「导出内容备份」中。需要恢复时，关闭工作台，将备份目录内的 `img/` 文件按原路径复制回仓库。普通上传不会覆盖已经替换过的同名素材。

替换后素材库会自动刷新，本地博客已打开的页面需要刷新查看。线上网站需在「云端同步」确认发布；若发布后仍显示旧图，可强制刷新或等待缓存更新。

### 文件保存位置

| 内容 | 保存位置 |
| --- | --- |
| 已收录评测 | `content/games/*.json` |
| 草稿 | `.blog-manager/draft/` |
| 回收站 | `.blog-manager/trash/` |
| 保存前的历史版本 | `.blog-manager/history/` |
| 上传图片 | `img/games/uploads/` |

`.blog-manager/` 已被 Git 忽略，生成器也会跳过该目录。不要强制提交它，里面可能有未公开内容。图片上传后位于公开素材目录，即使仅用于草稿也可能随下次 Git 推送公开；敏感图片请勿上传。

写入采用临时文件替换；编辑已有文章前会保存历史版本。同一文章被另一个窗口修改时，旧窗口的保存会被拒绝，请刷新并重新打开文章后再编辑。生成过程报错时会尝试恢复本次写入，不替代整库备份；断电或进程被强制结束时，请用备份或 Git 恢复。

服务仅监听 `127.0.0.1`，校验 Host/Origin 与写入令牌，拒绝直接访问隐藏目录及内容源文件；它不是可部署到公网的 CMS，不应做端口转发。后台前端没有外部依赖，本地博客预览仍会沿用原主题的 CDN 资源。

## 备份恢复

先关闭工作台，解压下载的 ZIP，选择需要恢复的文件按 ZIP 中的相对路径复制到仓库，重新启动工作台并生成。备份不包含程序和完整静态站点，程序仍由 Git 管理。恢复单个历史版本时，将 `.blog-manager/history/时间戳-文章路径.json` 复制到草稿或文章源目录，并重命名为 JSON 内的 `slug` 加 `.json`。同一 slug 不能同时存在于多个状态目录。

草稿、回收站和历史不会通过 Git 在电脑间同步，更换电脑前请下载备份。后台暂不提供直接还原 ZIP，以避免无提示覆盖文件。

## 当前范围

### 雾青配色

博客以 `#39c5bb` 为品牌点缀，搭配低饱和雾青 `#387f79`、暖白 `#fcfdf9`、灰绿背景 `#f2f5f1`；深色模式使用森林绿背景 `#182522` 与 `#22332f` 卡片。原背景图片保留，通过降饱和与遮罩降低干扰，正文截图、游戏封面和头像不做额外色相调整。

在工作台「样式与颜色」中调整基础色、圆角、字号等，配置保存至 `content/appearance.json`。`css/palette.css` 是全站语义色、深色模式和组件风格的源样式；生成器将它与基础配置合并为 `css/appearance.css`，并更新所有页面的缓存版本。请勿直接修改生成文件。工作台的「恢复默认」也使用这套雾青配色。

修改配色源文件后运行 `python scripts/build_games.py`，再运行 `python scripts/build_games.py --check` 检查一致性。生成仅修改本地文件，不会自动发布。

仓库是已生成的静态博客，没有 Hexo 源工程。本工具管理游戏评测、游戏作品、关于页面、图片和基础样式；评测的分类与标签沿用「游戏评测」。音乐等其他静态页目前仅支持预览，不提供任意 HTML 编辑。正文是轻量语法，不是完整 Markdown 编辑器。

如果之后用其他 Hexo 工程部署，需要将这里的内容及功能同步到源工程，避免覆盖。生成一致性检查：

```powershell
python scripts/build_games.py --check
```
